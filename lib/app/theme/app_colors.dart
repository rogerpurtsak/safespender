import 'package:flutter/material.dart';

class AppColors {
  // Brand / primary
  static const primary = Color.fromARGB(255, 11, 99, 93);
  static const primaryDark = Color(0xFF0A6E67);
  static const primarySoft = Color(0xFFDFF5F2);

  // Backgrounds / surfaces
  static const background = Color(0xF8F9FFFF);
  static const surface = Color.fromARGB(255, 255, 255, 255);
  static const surfaceSoft = Color.fromARGB(255, 255, 255, 255);

  // Text
  static const textPrimary = Color(0xFF0A2342);
  static const textSecondary = Color(0xFF7E8CA0);
  static const textMuted = Color(0xFFA7B1BF);

  // Borders / dividers
  static const border = Color.fromARGB(255, 235, 238, 243);
  static const divider = Color(0xFFDCE3EC);

  // Status
  static const success = Color(0xFF1C8C5E);
  static const successBg = Color(0xFFE7F7EF);

  static const warning = Color(0xFFC97A2B);
  static const warningBg = Color(0xFFFFF3E7);

  static const info = Color(0xFF3E7CB1);
  static const infoBg = Color(0xFFEAF3FB);

  static const error = Color(0xFFC94B4B);
  static const errorBg = Color(0xFFFCECEC);

  // Bottom navigation
  static const navBackground = Color(0xFFFFFFFF);
  static const navSelected = Color(0xFF8FE3D4);
  static const navSelectedIcon = Color(0xFF0E8C83);
  static const navUnselected = Color(0xFF98A3B3);

  // Inputs
  static const inputFill = Color.fromARGB(255, 238, 243, 248);
  static const inputBorder = Color(0xFFE7EDF3);

  // Cards / highlights
  static const cardHighlight = Color(0xFFF9FBFD);
}